---
workitem_name: my new project
container_name: dev
bwversion: 0.3.0
status: in_progress
code_path: /Users/stephen.hilton/Dev/bentwookie/test/test/test/testcode
infra_file: /some/path/infrastructure.md
started_at: '2026-02-11 15:52:16'
complete_at: ''
---
# PREAMBLE
**IMPORTANT**:
This is an automated coding agent running in headless mode, do not stop to ask questions.
When appropriate, build TASKS and consume multiple agents, up to 10.
This is running in a container, so feel free to install any dependencies you need, and to create any files you need.
When you have completed the INSTRUCTIONS, perform the FINAL TASKS, and then exit.
 
---

# INSTRUCTIONS
# PREAMBLE
{PREAMBLE}
 
---

# INSTRUCTIONS
create a tic-tac-toe game, in CLI form.  The human can play against a simple algorythm that tries to win, but is NOT ai (just picks the next best move). wrap the python file in a shell script, so I can just call ttt.sh.

--- 

# FINAL TASKS
**IMPORTANT**:
Before you conclude processing, you MUST complete the following tasks:
- perform unit test on any processes touched in the INSTRUCTIONS, and fix anything that fails. Always re-test after making a fix, and repeat until all tests pass.
- generate new sections to the end of 20260211_155153_my-new-project.md;
  - `# SUMMARY OF COMPLETED WORK` which includes a comprehensive, final summary of the work done, tests performed, pass status, etc.
  - `# NOTIFICATION` which is a 100-200 word message to be sent to relevant stakeholders, summarizing the work done, any testing anomalies, and any next steps that require attention. This should be written in a clear, concise manner, suitable for a semi-technical audience.
  - `# INFRASTRUCTURE` section, broken out into several sections, each comprehensive and detailed enough for another engineer to understand exactly what was changed and why:
    - `## REUSED` outlining what existing infrastructure was re-used.  Reused includes new configuration files, without changing core service settings (for example, a new kafka topic, without modifying any kafka service setting).  This is the preferred path, when possible.
    - `## UPDATED` outlining any existing infrastructure that was modified, how it was modified, and the detailed rationale for the modification.  When modifying existing infrastructure, always pause to ask yourself, "is this modification a requirement?".  Take care to minimize the blast radius of the change, and to ensure that the change is well tested, with a clear rollback plan in case of any issues.
    - `## NEW` outlining any new infrastructure that was created, how it was created, the detailed rationale outlining the strong need for new infrastructure, and an explanation of how it can be reused by other processes in the future. Before creating new infrastructure, always pause to ask yourself, "is this addition to our tech stack clearly worth the added complexity, cost, and risk?" Be sure the new infrastructure aligns well to any deployment patterns, such as cloud, containerization, etc.  After adding new infrastructure, always add a NEXT STEP to review past code and see if that new infrastructure can make existing processes more efficient, more hardened, or more scalable.
  - `# NEXT STEPS` which includes next steps for follow-on work. Each next step MUST be written inside a fenced block using the ```nextstep marker, with YAML content including at minimum an `instructions` field. For example:

    ```nextstep
    workitem_name: deploy-service
    code_path: /app/project
    type: required
    instructions: |-
      Deploy the service to the staging environment. Run integration tests
      against the staging endpoint and verify all health checks pass.
    ```

    ```nextstep
    workitem_name: optimize-queries
    code_path: /app/project
    type: recommended
    instructions: |-
      Review the database queries added in this workitem and look for
      optimization opportunities such as adding indexes or query caching.
    ```

    Break next steps into two subsections:
    - `## Required` — deployment, integration testing, performance testing, etc.
    - `## Recommended` — future improvements, optimizations, etc.
- at the very end, find the frontmatter section at the top of the 20260211_155153_my-new-project.md file, and update the `status` field to `complete`, and add a `complete_at` field with a timestamp of when you completed the FINAL TASKS.  This will trigger an automated notification to relevant stakeholders, so be sure to only update the status when you have truly completed all of the FINAL TASKS.

--- 

# FINAL TASKS
**IMPORTANT**:
Before you conclude processing, you MUST complete the following tasks:
- perform unit test on any processes touched in the INSTRUCTIONS, and fix anything that fails. Always re-test after making a fix, and repeat until all tests pass.
- generate new sections to the end of 20260211_155153_my-new-project.md;
  - `# SUMMARY OF COMPLETED WORK` which includes a comprehensive, final summary of the work done, tests performed, pass status, etc.
  - `# NOTIFICATION` which is a 100-200 word message to be sent to relevant stakeholders, summarizing the work done, any testing anomalies, and any next steps that require attention. This should be written in a clear, concise manner, suitable for a semi-technical audience.
  - `# INFRASTRUCTURE` section, broken out into several sections, each comprehensive and detailed enough for another engineer to understand exactly what was changed and why:
    - `## REUSED` outlining what existing infrastructure was re-used.  Reused includes new configuration files, without changing core service settings (for example, a new kafka topic, without modifying any kafka service setting).  This is the preferred path, when possible.
    - `## UPDATED` outlining any existing infrastructure that was modified, how it was modified, and the detailed rationale for the modification.  When modifying existing infrastructure, always pause to ask yourself, "is this modification a requirement?".  Take care to minimize the blast radius of the change, and to ensure that the change is well tested, with a clear rollback plan in case of any issues.
    - `## NEW` outlining any new infrastructure that was created, how it was created, the detailed rationale outlining the strong need for new infrastructure, and an explanation of how it can be reused by other processes in the future. Before creating new infrastructure, always pause to ask yourself, "is this addition to our tech stack clearly worth the added complexity, cost, and risk?" Be sure the new infrastructure aligns well to any deployment patterns, such as cloud, containerization, etc.  After adding new infrastructure, always add a NEXT STEP to review past code and see if that new infrastructure can make existing processes more efficient, more hardened, or more scalable.
  - `# NEXT STEPS` which includes next steps for follow-on work. Each next step MUST be written inside a fenced block using the ```nextstep marker, with YAML content including at minimum an `instructions` field. For example:

    ```nextstep
    workitem_name: deploy-service
    code_path: /app/project
    type: required
    instructions: |-
      Deploy the service to the staging environment. Run integration tests
      against the staging endpoint and verify all health checks pass.
    ```

    ```nextstep
    workitem_name: optimize-queries
    code_path: /app/project
    type: recommended
    instructions: |-
      Review the database queries added in this workitem and look for
      optimization opportunities such as adding indexes or query caching.
    ```

    Break next steps into two subsections:
    - `## Required` — deployment, integration testing, performance testing, etc.
    - `## Recommended` — future improvements, optimizations, etc.
- at the very end, find the frontmatter section at the top of the 20260211_155153_my-new-project.md file, and update the `status` field to `complete`, and add a `complete_at` field with a timestamp of when you completed the FINAL TASKS.  This will trigger an automated notification to relevant stakeholders, so be sure to only update the status when you have truly completed all of the FINAL TASKS.

 